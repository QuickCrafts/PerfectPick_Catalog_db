CREATE TABLE movies (
    id_movie text PRIMARY KEY,
    awards text,
    cast text,
    director text,
    duration text,
    episodes int,
    genre text,
    original_title text,
    rating decimal,
    release_date date,
    seasons int,
    title text,
    writers text
);